# 🎯 دليل Debugging الشامل - Quick Start

## 📚 الملفات المتوفرة

### 1. **DEBUGGING_WORKFLOW.md** - الدليل الشامل
   - كل مرحلة من Git حتى Browser
   - خطوات التشخيص التفصيلية
   - أمثلة عملية

### 2. **ERROR_MAPPING.md** - خريطة الأخطاء
   - جدول: Error → السبب → الحل
   - تفاصيل الأخطاء الشائعة
   - Quick Reference

### 3. **DEPLOY_CHECKLIST.md** - قائمة التحقق
   - قبل Push
   - بعد Push
   - بعد Deploy
   - Checklist سريعة

### 4. **PREVENTIVE_DEBUGGING.md** - منع الأخطاء
   - أفضل الممارسات
   - تنظيم المشروع
   - Naming conventions
   - Cache management

---

## 🚀 البدء السريع

### إذا واجهت خطأ:

1. **افتح `ERROR_MAPPING.md`** → ابحث عن رسالة الخطأ
2. **اتبع الحل المذكور**
3. **إذا لم تجد الحل** → افتح `DEBUGGING_WORKFLOW.md`

### قبل كل Deploy:

1. **افتح `DEPLOY_CHECKLIST.md`**
2. **اتبع القائمة** خطوة بخطوة
3. **اختبر باستخدام `test.html`**

### لتنظيم المشروع:

1. **افتح `PREVENTIVE_DEBUGGING.md`**
2. **اتبع أفضل الممارسات**
3. **نظّم المشروع من البداية**

---

## 🔍 سيناريوهات شائعة

### السيناريو 1: "الموقع لا يعمل بعد Deploy"

```
1. افتح: https://yourdomain.com/test.html
   → إذا لم يعمل → المشكلة في Deploy
   
2. Hostinger → Git → Recent Deployments
   → تحقق من آخر deployment
   
3. File Manager → public_html/
   → تحقق من تاريخ الملفات
   
4. إذا الملفات قديمة → Deploy فشل
   → تحقق من Webhook
```

### السيناريو 2: "403 Forbidden"

```
1. File Manager → public_html/index.html
   → Permissions → 644
   
2. File Manager → public_html/
   → Permissions → 755
   
3. إذا استمر → احذف .htaccess مؤقتاً
```

### السيناريو 3: "تغييرات لا تظهر"

```
1. Browser → Hard Refresh: Ctrl+Shift+R
   
2. DevTools (F12) → Network tab
   → ✅ Enable "Disable cache"
   
3. إذا استمر → المشكلة في Server
   → تحقق من File Manager → تاريخ الملفات
```

---

## 📋 Checklist سريعة (30 ثانية)

```
✅ Git status نظيف
✅ على branch main
✅ Push نجح
✅ Webhook: 200 OK
✅ الموقع يعمل
✅ Hard Refresh → التغييرات ظاهرة
```

---

## 🛠️ الأدوات المساعدة

### 1. صفحة الاختبار
```
https://yourdomain.com/test.html
```
- تتحقق من HTML, CSS, JavaScript, API
- تظهر الوقت الحالي
- مفيدة بعد كل deploy

### 2. API Test Connection
```
https://yourdomain.com/api/test-connection.php
```
- يتحقق من الإعدادات
- يتحقق من قاعدة البيانات
- يتحقق من الصلاحيات

### 3. سكريبت الاختبار (Linux/Mac)
```bash
./scripts/test-deploy.sh yourdomain.com
```
- يختبر الملفات الأساسية
- يختبر API
- يعرض تقرير سريع

---

## 📖 الترتيب الموصى به للقراءة

### للمبتدئين:
1. `DEPLOY_CHECKLIST.md` - ابدأ من هنا
2. `ERROR_MAPPING.md` - للأخطاء الشائعة
3. `DEBUGGING_WORKFLOW.md` - للتفاصيل

### للمحترفين:
1. `PREVENTIVE_DEBUGGING.md` - أفضل الممارسات
2. `DEBUGGING_WORKFLOW.md` - للتفاصيل التقنية
3. `ERROR_MAPPING.md` - كمرجع سريع

---

## 🎯 الخلاصة

**القاعدة الذهبية:**
> ابدأ من الأسفل (Browser) للأعلى (Git)

**الخطوات الأساسية:**
1. Hard Refresh أولاً
2. تحقق من DevTools
3. تحقق من File Manager
4. تحقق من Logs
5. تحقق من Git/Webhook

**الملفات المهمة:**
- `test.html` - للاختبار السريع
- `api/test-connection.php` - للتشخيص
- `DEBUGGING_WORKFLOW.md` - للدليل الكامل

---

## 📞 مساعدة إضافية

إذا استمرت المشاكل:
1. راجع `DEBUGGING_WORKFLOW.md` → القسم المناسب
2. راجع `ERROR_MAPPING.md` → جدول الأخطاء
3. استخدم `api/test-connection.php` → للتشخيص
4. تحقق من Error Logs في cPanel

---

**آخر تحديث:** 2025-01-08
