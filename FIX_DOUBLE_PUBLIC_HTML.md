# 🔧 إصلاح مشكلة public_html/public_html

## المشكلة

الملفات موجودة في `public_html/public_html` على السيرفر بدلاً من `public_html/` فقط.

**السبب:**
- المشروع فيه مجلد `public_html/`
- Install Path في Hostinger مضبوط على `/public_html`
- النتيجة: الملفات تروح على `/public_html/public_html`

---

## ✅ الحل السريع (الأسهل)

### الخطوة 1: تغيير Install Path في Hostinger

1. اذهب إلى **Hostinger Control Panel**
2. ابحث عن **"Git"** أو **"Version Control"**
3. افتح إعدادات Repository
4. غيّر **Install Path** من `/public_html` إلى `/` (الجذر)
5. احفظ التغييرات

### الخطوة 2: تنظيف الملفات القديمة

1. في **File Manager** على Hostinger
2. اذهب إلى `/public_html/public_html/`
3. **احذف** هذا المجلد (أو انقله كنسخة احتياطية)
4. أو انقل محتوياته إلى `/public_html/` إذا كان فيه ملفات مهمة

### الخطوة 3: إعادة Deploy

1. في Hostinger Git → اضغط **"Deploy Now"** (Manual Deploy)
2. أو اعمل `git push` جديد
3. تحقق من أن الملفات الآن في `/public_html/` مباشرة

---

## ✅ الحل البديل (أكثر تنظيماً)

إذا أردت إبقاء Install Path على `/public_html`:

### الخطوة 1: نقل الملفات في المشروع

```bash
# في المشروع المحلي
# انقل كل شيء من public_html/ إلى الجذر
```

لكن هذا يحتاج تعديلات كثيرة في المسارات.

**الأفضل:** استخدم الحل الأول (تغيير Install Path إلى `/`)

---

## 🔍 التحقق من الحل

بعد التغيير:

1. **File Manager** → `/public_html/`
2. يجب أن ترى:
   - `index.html` ✅
   - `api/` ✅
   - `assets/` ✅
   - `templates/` ✅
   - إلخ...

3. **لا يجب** أن ترى:
   - `public_html/` داخل `/public_html/` ❌

---

## 📋 Checklist

- [ ] غيّرت Install Path إلى `/` في Hostinger
- [ ] حذفت أو نقلت `/public_html/public_html/`
- [ ] عملت Deploy جديد
- [ ] تحققت من أن الملفات في المكان الصحيح
- [ ] اختبرت الموقع: `https://yourdomain.com/`

---

## ⚠️ ملاحظة مهمة

بعد تغيير Install Path:
- الملفات ستكون في `/public_html/` مباشرة
- المسارات في الكود ستعمل بشكل صحيح
- لا حاجة لتعديل أي ملفات

---

## 🎯 الخلاصة

**الحل الأسهل والأسرع:**
1. Hostinger → Git → Install Path: `/` (بدلاً من `/public_html`)
2. Deploy جديد
3. ✅ انتهى!
