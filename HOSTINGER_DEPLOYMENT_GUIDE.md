# 🚀 دليل النشر على Hostinger - Hostinger Deployment Guide

## 📐 بنية المشروع الصحيحة

### في GitHub Repository:

```
project/
├── public_html/              # جميع ملفات النشر هنا
│   ├── index.html
│   ├── api/
│   ├── assets/
│   ├── templates/
│   ├── storage/
│   ├── includes/
│   ├── config/
│   └── ...
├── scripts/                  # سكريبتات مساعدة (لا تُنشر)
├── .gitignore
├── README.md
└── ...
```

### على Hostinger Server (بعد Deploy):

```
/public_html/                 # هذا هو الجذر على السيرفر
├── index.html               ✅ مباشرة (ليس في public_html/)
├── api/                     ✅ مباشرة
├── assets/                  ✅ مباشرة
├── templates/               ✅ مباشرة
├── storage/                 ✅ مباشرة
├── includes/                ✅ مباشرة
├── config/                  ✅ مباشرة
└── ...
```

---

## ⚙️ إعدادات Hostinger Git

### الإعدادات الصحيحة:

```
┌─────────────────────────────────────────┐
│ Hostinger Git Settings                  │
├─────────────────────────────────────────┤
│ Repository URL:                         │
│ https://github.com/USERNAME/REPO.git   │
│                                         │
│ Branch:                                 │
│ main                                    │
│                                         │
│ Install Path:                           │
│ /                    ⚠️ الجذر وليس     │
│                      /public_html       │
│                                         │
│ Auto Deploy:                            │
│ ✅ Enabled                              │
└─────────────────────────────────────────┘
```

### كيف يعمل:

1. **GitHub Repository** فيه: `public_html/index.html`
2. **Install Path** = `/` (الجذر)
3. **Hostinger** ينشر محتويات المشروع في `/`
4. بما أن المشروع فيه `public_html/`
5. **النتيجة:** `/public_html/index.html` ✅

---

## 🔍 التحقق من البنية

### بعد كل Deploy:

#### 1. في File Manager:

```
/public_html/
├── index.html          ✅ يجب أن يكون مباشرة
├── api/                ✅ مباشرة
├── assets/             ✅ مباشرة
└── ...

❌ لا يجب أن ترى:
/public_html/public_html/  ❌
```

#### 2. في Browser:

```
✅ https://yourdomain.com/              → يعمل
✅ https://yourdomain.com/test.html     → يعمل
✅ https://yourdomain.com/api/...       → يعمل

❌ https://yourdomain.com/public_html/  → لا يجب أن يعمل
```

#### 3. في DevTools:

```
Browser → DevTools (F12) → Network tab
✅ جميع الملفات تحمل من المسارات الصحيحة
✅ لا توجد أخطاء 404
```

---

## 🛠️ إصلاح المشكلة (إذا حدثت)

### الخطوة 1: تغيير Install Path

```
Hostinger → Git → Version Control
→ Install Path: / (الجذر)
→ Save
```

### الخطوة 2: تنظيف الملفات القديمة

```
File Manager → /public_html/public_html/
→ Delete (أو Move ك backup)
```

### الخطوة 3: إعادة Deploy

```
Hostinger → Git → Deploy Now
أو
git push origin main
```

### الخطوة 4: التحقق

```
File Manager → /public_html/
✅ الملفات مباشرة (ليس في public_html/)
```

---

## 📋 Checklist سريعة

### قبل Push:

- [ ] بنية المشروع صحيحة: `public_html/` في الجذر
- [ ] Install Path في Hostinger = `/` (الجذر)
- [ ] `.gitignore` صحيح

### بعد Deploy:

- [ ] File Manager → `/public_html/` → الملفات مباشرة
- [ ] لا يوجد `/public_html/public_html/`
- [ ] الموقع يعمل: `https://yourdomain.com/`
- [ ] جميع المسارات تعمل

---

## 🎯 القاعدة الذهبية

```
Install Path في Hostinger = /
+
المشروع فيه public_html/
=
النتيجة: /public_html/ ✅

---

Install Path في Hostinger = /public_html
+
المشروع فيه public_html/
=
النتيجة: /public_html/public_html/ ❌
```

---

## 📞 استكشاف الأخطاء

### المشكلة: الملفات في `/public_html/public_html/`

**السبب:** Install Path = `/public_html`

**الحل:**
1. غيّر Install Path إلى `/`
2. احذف `/public_html/public_html/`
3. إعادة Deploy

### المشكلة: الملفات في الجذر `/` مباشرة

**السبب:** Install Path = `/` والمشروع بدون `public_html/`

**الحل:**
- هذا صحيح إذا كان المشروع بدون `public_html/`
- أو غيّر Install Path إلى `/public_html`

---

**آخر تحديث:** 2025-01-08
